<%= form_with model: @employee_type do |f| %>
  <div class="form-group">
    <%= f.text_field :title, class: 'form-control', placeholder: 'Cargo' %>
  </div>
  <%= f.submit class: 'btn btn-primary'%>
<% end %>